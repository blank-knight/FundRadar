# FundRadar 环境变量示例

# 本地开发时配置（复制为 .env.local）
# VITE_API_URL=http://localhost:8001/api

# Vercel 部署时：
# - 如果后端已部署，在 Vercel 项目设置 → Environment Variables 配置 VITE_API_URL
# - 如果不配（纯前端 Demo），所有页面自动使用 Mock 数据展示
